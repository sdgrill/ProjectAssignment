<!DOCTYPE html>
<html>
  <%- include ./partials/head %>

  <body>
    <% include ./partials/navbar %>

    <div class="container">
      <h1>Welcome to the ILMP's Home Page</h1>
      
      <% if (locals.user) %>
        <p>Hi <%= locals.user.firstName %> <%= locals.user.lastName%>!</p>

      <!--<% if (locals.user.role === 'Admin') { %>
      <br>
        <a href="/users">Go to Users</a>
      <% } %>-->
    </div>
    </div>
    <% include ./partials/scripts %>
  </body>
</html>
